
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.7.0
  creationTimestamp: null
  name: modelversions.model.distributed.io
spec:
  group: model.distributed.io
  names:
    kind: ModelVersion
    listKind: ModelVersionList
    plural: modelversions
    shortNames:
    - mv
    singular: modelversion
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.modelName
      name: Model
      type: string
    - jsonPath: .status.image
      name: Image
      type: string
    - jsonPath: .spec.createdBy
      name: Created-By
      type: string
    - jsonPath: .status.finishTime
      name: Finish-Time
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ModelVersion is the Schema for the modelversions API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ModelVersionSpec defines the desired state of ModelVersion.
              Each time a new ModelVersion crd is created, the corresponding controller
              will create an image that incorporates the model.
            properties:
              createdBy:
                description: CreatedBy indicates the entity that creates the model
                  version. e.g. the name of the torchjob that creates the ModelVersion,
                  or the name of the user that creates the ModelVersion.
                type: string
              imageRepo:
                description: ImageRepo is the image repository to push the generated
                  model image, e.g., "docker.io/some-model". A particular image tag
                  is automatically generated by the system when ImageTag is nil, e.g.,
                  "docker.io/some-model:v1".
                type: string
              imageTag:
                description: ImageTag is the image tag suffixed with image repo, which
                  indicates a specific image version/variant. If empty, the first
                  5 digits of this ModelVersion object UID will be used as image tag.
                type: string
              modelName:
                description: The model name for this ModelVersion
                type: string
              storage:
                description: Storage is the location where this ModelVersion is stored.
                properties:
                  localStorage:
                    description: LocalStorage represents the local host storage
                    properties:
                      mountPath:
                        description: The mounted path inside the container. The training
                          code is expected to export the model artifacts under this
                          path.
                        type: string
                      nodeName:
                        description: The name of the node for storing the model.
                        type: string
                      path:
                        description: The local host path to export the model.
                        type: string
                    type: object
                  nfs:
                    description: NFS represents the nas storage
                    properties:
                      mountPath:
                        description: The mounted path inside the container. The training
                          code is expected to export the model artifacts under this
                          path, such as storing the tensorflow saved_model.
                        type: string
                      path:
                        description: The path under which the model is stored, e.g.
                          /models/my_model1
                        type: string
                      server:
                        description: NFS server address
                        type: string
                    type: object
                type: object
            type: object
          status:
            description: ModelVersionStatus defines the observed state of ModelVersion
            properties:
              finishTime:
                description: FinishTime is the time when image building is finished.
                format: date-time
                type: string
              image:
                description: The image name of the model version
                type: string
              imageBuildPhase:
                description: ImageBuildPhase is the phase of the image building process
                type: string
              message:
                description: Any message associated with the building process
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
