
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.7.0
  creationTimestamp: null
  name: models.model.distributed.io
spec:
  group: model.distributed.io
  names:
    kind: Model
    listKind: ModelList
    plural: models
    shortNames:
    - mo
    singular: model
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.latestVersion.modelVersion
      name: Latest-Version
      type: string
    - jsonPath: .status.latestVersion.imageName
      name: Latest-Image
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Model is the Schema for the models API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ModelSpec defines the desired state of Model
            properties:
              description:
                description: Description is an arbitrary string that describes what
                  this model for.
                type: string
            type: object
          status:
            description: ModelStatus defines the observed state of Model
            properties:
              latestVersion:
                description: LatestVersion indicates the latest ModelVersion
                properties:
                  imageName:
                    description: The image name of the latest model,  e.g. "docker.io/some-model:v1"
                    type: string
                  modelVersion:
                    description: The name of the latest ModelVersion
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
